# Semantics MKCC

This [test](.) is exercising the semantics rule `MKCC`.

### Description

In this test, an instance of the once class `BB` is cloned. The cloned object
still matches the same branch in an inspect instruction. This test satisfies `MKCC`.
